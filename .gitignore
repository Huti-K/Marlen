node_modules/
dist/
*.local
.env
.env.*
!.env.example
# Runtime state: SQLite DB + stored credentials (auth.json)
apps/server/data/
# Assembled desktop app (bundles + staged node_modules); installers land in
# apps/desktop/dist/, covered by the global dist/ rule above.
apps/desktop/build/
.claude/settings.local.json
.DS_Store
*.log
*.db
*.db-wal
*.db-shm
*.db-journal
CLAUDE.md
CONTEXT.md
# Graphify knowledge-graph index (local dev tool output)
graphify-out/

# Local agent rules — deliberately unpublished
AGENTS.md
REFACTOR.md
