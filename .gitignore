node_modules/
dist/
*.local
.env
.env.*
!.env.example
# Runtime state: SQLite DB + stored credentials (auth.json)
apps/server/data/
# Agent home (memory/, skills/, knowledge/); the desktop app keeps it under userData
agent-home/
# Assembled desktop app (bundles + staged node_modules); installers land in
# apps/desktop/dist/, covered by the global dist/ rule above.
apps/desktop/build/
.claude/settings.local.json
.DS_Store
*.log
*.db
*.db-wal
*.db-shm
*.db-journal
CONTEXT.md

# Local refactor planning doc — deliberately unpublished
REFACTOR.md

# Per-customer install bundles (agent-home layout + setup skill) — private, not for the public repo
setup-template/

# Local demo-inbox seeding tool (IMAP-appends fixture mail into a test Gmail) — not for the public repo
demo-seed/
*.tsbuildinfo
